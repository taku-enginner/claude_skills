# Flutter テスト自動化 使い方ガイド

Flutterプロジェクトでの実装→テスト→修正の自動化フローです。

---

## 目次

1. [全体像](#全体像)
2. [セットアップ](#セットアップ)
3. [基本的な使い方](#基本的な使い方)
4. [実際の動作例](#実際の動作例)
5. [個別スキルの使い方](#個別スキルの使い方)
6. [生成されるテストの種類](#生成されるテストの種類)
7. [よくある使い方](#よくある使い方)
8. [自動フローの詳細](#自動フローの詳細)
9. [テストケースのカスタマイズ](#テストケースのカスタマイズ)
10. [トラブルシューティング](#トラブルシューティング)

---

## 全体像

```
┌─────────────────────────────────────────────────────┐
│  あなたのFlutterプロジェクト                         │
│  my_app/                                            │
│  ├── lib/                                           │
│  ├── test/                                          │
│  ├── CLAUDE.md → (リンク)                           │
│  ├── .claude/ → (リンク)                            │
│  └── .claude_skills/ → (リンク)                     │
└─────────────────────────────────────────────────────┘
                    ↑
                 リンク
                    │
┌─────────────────────────────────────────────────────┐
│  claude_skills/（このリポジトリ）                    │
│  ├── CLAUDE.md           ← ルール定義               │
│  ├── .claude/commands/   ← スラッシュコマンド       │
│  └── skills/code/        ← スキル本体               │
└─────────────────────────────────────────────────────┘
```

### できること

「〇〇を実装して」と指示するだけで、Claude Codeが:

1. 機能を実装
2. テストケースを自動生成
3. テストを実行
4. 失敗したら修正して再テスト
5. 成功するまで繰り返し（最大3回）
6. 3回失敗したらユーザーに相談

を自動で行います。

---

## セットアップ

### Step 1: このリポジトリをクローン

```bash
cd ~
git clone https://github.com/your-repo/claude_skills.git
```

### Step 2: Flutterプロジェクトにリンク

```bash
# あなたのFlutterプロジェクトに移動
cd /path/to/my_flutter_app

# セットアップスクリプトを実行
~/claude_skills/setup.sh .
```

### Step 3: 確認

```bash
ls -la
```

以下が表示されればOK:
```
CLAUDE.md -> /home/you/claude_skills/CLAUDE.md
.claude -> /home/you/claude_skills/.claude
.claude_skills -> /home/you/claude_skills/skills
```

### Step 4: 依存パッケージ確認

```bash
# flutter_test は通常デフォルトで含まれています
flutter pub get
```

---

## 基本的な使い方

### パターンA: 自然言語で指示（推奨）

Claude Codeを起動して、普通に話しかけるだけ：

```
あなた: メールアドレスのバリデーション関数を作って
```

Claude Codeが自動で:
1. `lib/utils/validator.dart` を作成
2. `test/utils/validator_test.dart` を生成
3. テストを実行
4. 全テストが通るまで修正

### パターンB: スラッシュコマンド

```
/project:implement ユーザー登録のバリデーション
/project:generate-tests lib/utils/validator.dart
/project:run-tests test/utils/validator_test.dart
```

### パターンC: 個別にスキルを使う

```
あなた: lib/utils/validators.dart のテストを書いて

Claude Code: テストを生成します...
```

```
あなた: テストを実行して

Claude Code: flutter test を実行します...
```

---

## 実際の動作例

### 例1: 新機能の実装

```
あなた: 電話番号のバリデーション関数を作って。
        日本の電話番号形式（090-1234-5678）に対応して。
```

**Claude Codeの動作:**

```
1. 実装中...

lib/utils/phone_validator.dart を作成:
```

```dart
bool validatePhoneNumber(String phone) {
  final regex = RegExp(r'^0[789]0-\d{4}-\d{4}$');
  return regex.hasMatch(phone);
}
```

```
2. テスト生成中...

test/utils/phone_validator_test.dart を作成
  - 8件のテストケースを生成

3. テスト実行中...

実行コマンド: flutter test test/utils/phone_validator_test.dart

============================================================
テスト実行レポート
============================================================

  ✅ 合計: 8件
  ✅ 成功: 7件
  ❌ 失敗: 1件

## 失敗したテスト
  1. validatePhoneNumber_ハイフンなし_falseを返す
     Expected: false
     Actual: true

4. 修正中...（1回目）

validatePhoneNumber にハイフンなし形式のチェックを追加

5. 再テスト...

  ✅ 合計: 8件
  ✅ 成功: 8件

## 実装完了

### 実装内容
- validatePhoneNumber() 関数
- 090/080/070 形式に対応
- ハイフンあり/なし両方をチェック

### ファイル
- lib/utils/phone_validator.dart (新規)
- test/utils/phone_validator_test.dart (新規)

### テスト結果
- 成功: 8件 ✅
```

---

### 例2: 3回失敗した場合

```
あなた: 国際電話番号のバリデーションを作って
```

**Claude Codeの動作:**

```
...（実装・テスト・修正を3回繰り返し）...

## テストが3回失敗しました

### 失敗しているテスト
- validateInternationalPhone_プラス記号なし_falseを返す
  Expected: false, Actual: true

### 試した修正
1. 正規表現に ^ を追加 → 別のテストが失敗
2. プラス記号を必須に → 国内番号が通らなくなった
3. 国コード判定を追加 → 複雑になりすぎてエラー

### 考えられる原因
- 国際電話番号の形式が多様すぎる
- 国内番号と国際番号の判定基準が不明確

### 質問
以下のどの形式をサポートしますか？

1. +81-90-1234-5678（国コード付き）のみ
2. 090-1234-5678（国内形式）も許可
3. 両方をサポート（引数で切り替え）

どのように対応しますか？
```

```
あなた: 3番で、引数で切り替えられるようにして

Claude Code: 了解しました。引数 allowDomestic を追加して実装します...
```

---

## 個別スキルの使い方

### 1. Dartコード解析 (analyze_dart.py)

Dartファイルの関数・クラス構造を解析します。

```bash
# 単一ファイル
python .claude_skills/code/analyze_dart.py lib/utils/validator.dart

# ディレクトリ全体
python .claude_skills/code/analyze_dart.py lib/ --recursive

# JSON出力
python .claude_skills/code/analyze_dart.py lib/models/user.dart --json
```

**出力例:**

```
============================================================
Dart解析結果: lib/utils/validator.dart
============================================================

import: 2件

トップレベル関数: 3件
  - bool validateEmail(String email) [line 5]
      /// メールアドレスを検証します...
  - bool validatePhone(String phone) [line 15]
  - String formatName(String name) [line 25]

クラス: 1件
  - class Validator [line 30]
      メソッド: 2件 (public), 1件 (private)
        - validate(String input)
        - isValid()

------------------------------------------------------------
テスト対象候補
------------------------------------------------------------
  トップレベル関数: 3件
  クラスメソッド: 2件
  合計: 5件
```

### 2. テスト生成 (generate_tests.py)

Dartファイルからテストコードを自動生成します。

```bash
# テストを生成
python .claude_skills/code/generate_tests.py lib/utils/validator.dart

# プレビューのみ（ファイル作成なし）
python .claude_skills/code/generate_tests.py lib/utils/validator.dart --dry-run

# 出力先を指定
python .claude_skills/code/generate_tests.py lib/utils/validator.dart --output test/custom_test.dart

# 既存ファイルを上書き
python .claude_skills/code/generate_tests.py lib/utils/validator.dart --force
```

**オプション:**

| オプション | 説明 |
|------------|------|
| `--dry-run` | ファイルを作成せずプレビュー |
| `--output <path>` | 出力先を指定 |
| `--force` | 既存ファイルを上書き |

### 3. テスト実行 (run_tests.py)

Flutterテストを実行してレポートを生成します。

```bash
# 全テスト実行
python .claude_skills/code/run_tests.py

# 特定のテストファイル
python .claude_skills/code/run_tests.py test/utils/validator_test.dart

# カバレッジ付き
python .claude_skills/code/run_tests.py --coverage

# 詳細出力
python .claude_skills/code/run_tests.py --verbose

# JSON出力
python .claude_skills/code/run_tests.py --json
```

**出力例:**

```
============================================================
テスト実行レポート
============================================================

実行日時: 2026-01-26T15:30:00
コマンド: flutter test test/utils/validator_test.dart

## サマリー
────────────────────────────────────────
  ✅ 合計: 12件
  ✅ 成功: 11件
  ❌ 失敗: 1件

  成功率: 91.7%
  実行時間: 2.35秒

## 失敗したテスト (1件)
────────────────────────────────────────

  1. validateEmail > validateEmail_日本語ドメイン_falseを返す
     📍 test/utils/validator_test.dart:45
     ❌ Expected: false
     ❌ Actual: true

## 推奨アクション
────────────────────────────────────────
  1. 失敗したテストのエラーメッセージを確認
  2. 該当するソースコードを修正
  3. テストを再実行して確認

  最初に確認すべきファイル:
    test/utils/validator_test.dart:45
```

---

## 生成されるテストの種類

| カテゴリ | 説明 | 例 |
|----------|------|-----|
| **正常系** | 有効な入力での動作確認 | `"test@example.com"` → `true` |
| **境界値** | 空文字、ゼロ、空リスト等 | `""` → `false` |
| **異常系** | null、不正な型など | `null` → 例外 |
| **セキュリティ** | XSS、SQLインジェクション | `"<script>"` → サニタイズ |

### 生成されるテストコードの例

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:app_name/utils/validator.dart';

void main() {
  group('validateEmail', () {
    test('validateEmail_正常なメール_trueを返す', () {
      // Arrange
      final email = "test@example.com";

      // Act
      final result = validateEmail(email);

      // Assert
      expect(result, isTrue);
    });

    test('validateEmail_空文字_falseを返す', () {
      // Arrange
      final email = "";

      // Act
      final result = validateEmail(email);

      // Assert
      expect(result, isFalse);
    });

    test('validateEmail_XSS文字列_安全に処理', () {
      // Arrange
      final email = "<script>alert(1)</script>";

      // Act
      final result = validateEmail(email);

      // Assert
      expect(result, isFalse);
    });
  });
}
```

---

## よくある使い方

### 「テストだけ書いて」

```
あなた: lib/models/user.dart のテストを書いて。
        実装は変更しないで。
```

### 「テストを実行して結果を教えて」

```
あなた: test/ のテストを全部実行して
```

### 「カバレッジを確認して」

```
あなた: テストを実行してカバレッジを確認して
```

### 「失敗しているテストを直して」

```
あなた: テストが失敗してるから直して
```

### 「このファイルを解析して」

```
あなた: lib/services/auth_service.dart の構造を教えて
```

---

## 自動フローの詳細

### フロー図

```
ユーザー: 「〇〇を実装して」
         │
         ▼
┌─────────────────────┐
│  1. 実装            │
│  lib/xxx.dart 作成  │
└─────────┬───────────┘
         │
         ▼
┌─────────────────────┐
│  2. テスト生成      │
│  test/xxx_test.dart │
└─────────┬───────────┘
         │
         ▼
┌─────────────────────┐
│  3. テスト実行      │
│  flutter test       │
└─────────┬───────────┘
         │
    ┌────┴────┐
    │ 結果は？ │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
 成功      失敗
    │         │
    ▼         ▼
┌───────┐ ┌─────────────┐
│ 完了  │ │ 修正        │
│ 報告  │ │ (最大3回)   │
└───────┘ └──────┬──────┘
                │
           ┌────┴────┐
           │ 3回目？ │
           └────┬────┘
                │
           Yes  │  No
                │
    ┌───────────┴───────────┐
    │                       │
    ▼                       ▼
┌─────────┐          ┌──────────┐
│ ユーザー │          │ 再テスト │
│ に相談  │          │ (3へ戻る) │
└─────────┘          └──────────┘
```

### 完了報告のフォーマット

```
## 実装完了

### 実装内容
- [実装した機能の説明]

### 作成/変更したファイル
- lib/xxx.dart (新規作成)
- test/xxx_test.dart (新規作成)

### テスト結果
- 合計: X件
- 成功: X件 ✅
- 失敗: 0件

### 次のステップ
[必要に応じて]
```

### 3回失敗時の相談フォーマット

```
## テストが3回失敗しました

### 失敗しているテスト
- [テスト名]: [エラー内容]

### 試した修正
1. [1回目の修正内容]
2. [2回目の修正内容]
3. [3回目の修正内容]

### 考えられる原因
- [原因1]
- [原因2]

### 質問
[具体的な質問や確認事項]

どのように対応しますか？
```

---

## テストケースのカスタマイズ

生成されたテストは自動生成のテンプレートです。
必要に応じて以下を追加してください。

### モックの追加

```dart
import 'package:mockito/mockito.dart';

class MockApiClient extends Mock implements ApiClient {}

void main() {
  late MockApiClient mockClient;

  setUp(() {
    mockClient = MockApiClient();
  });

  // ...
}
```

### 非同期テスト

```dart
test('fetchUser returns user', () async {
  // Arrange
  when(mockClient.get(any)).thenAnswer((_) async => Response(data: userData));

  // Act
  final result = await userRepository.fetchUser('123');

  // Assert
  expect(result.name, equals('John'));
});
```

### Widget テスト

```dart
testWidgets('LoginButton shows loading', (tester) async {
  await tester.pumpWidget(MaterialApp(home: LoginButton()));

  await tester.tap(find.byType(ElevatedButton));
  await tester.pump();

  expect(find.byType(CircularProgressIndicator), findsOneWidget);
});
```

---

## ディレクトリ構成

セットアップ後のプロジェクト構成:

```
my_flutter_app/
├── lib/
│   └── utils/
│       └── validators.dart      ← 実装
├── test/
│   └── utils/
│       └── validators_test.dart ← 自動生成されるテスト
├── pubspec.yaml
├── CLAUDE.md                    ← リンク（自動フロー定義）
├── .claude/                     ← リンク（コマンド）
│   └── commands/
│       ├── implement.md
│       ├── generate-tests.md
│       └── run-tests.md
└── .claude_skills/              ← リンク（スキル本体）
    └── code/
        ├── analyze_dart.py
        ├── generate_tests.py
        └── run_tests.py
```

---

## トラブルシューティング

### Flutterが見つからない

```
エラー: Flutterが見つかりません
```

**解決策:**
```bash
# PATHにFlutterを追加
export PATH="$PATH:/path/to/flutter/bin"

# 確認
flutter --version
```

### テストがタイムアウト

```
テストがタイムアウトしました（5分）
```

**解決策:**
- テスト対象を絞る: `python .claude_skills/code/run_tests.py test/specific_test.dart`
- 重い処理をモック化
- タイムアウトを延長（コード修正が必要）

### パッケージが見つからない

```
Error: Could not find package 'flutter_test'
```

**解決策:**
```bash
flutter pub get
```

### セットアップスクリプトが動かない

```
Permission denied
```

**解決策:**
```bash
chmod +x ~/claude_skills/setup.sh
```

### 既存のCLAUDE.mdがある

セットアップ時に既存の`CLAUDE.md`は`CLAUDE.md.backup`にリネームされます。
必要に応じて内容をマージしてください。

---

## FAQ

### Q: テストを生成せずに実装だけしてほしい

```
あなた: メールバリデーション関数を実装して。テストは不要。
```

### Q: 生成されたテストを編集したい

自動生成されたテストは `test/` ディレクトリに通常のDartファイルとして保存されます。
自由に編集できます。

### Q: 特定のテストだけ実行したい

```
あなた: test/utils/validator_test.dart だけ実行して
```

または

```bash
python .claude_skills/code/run_tests.py test/utils/validator_test.dart
```

### Q: カバレッジレポートはどこ？

```bash
python .claude_skills/code/run_tests.py --coverage
```

実行後、`coverage/lcov.info` に出力されます。
HTMLレポートを見たい場合:

```bash
genhtml coverage/lcov.info -o coverage/html
open coverage/html/index.html
```
